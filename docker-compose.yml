services:
  postgres:
    image: postgres:17
    container_name: ratinglists-postgres
    restart: unless-stopped
    env_file:
      - ./RatingLists_Backend/.env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
